---
title: "群友们提到的东西"
description: "love to read their logs"
image: /images/blog/
date: "2023-12-05"
authors:
  - Kud
---
[cachix](https://docs.cachix.org/getting-started)
> 大概是自建cache server？

[grapheneos](https://grapheneos.org/faq#supported-devices)
[grapheneos blog](https://tchncsde.github.io/2023/11/11/GarpheneOS-Security.html)
> 不太理解，这玩意只能在pixel上刷，为什么？

Nix 系统中存在两套命令行接口（CLI）模式，这通常指的是传统的 Nix 命令（如 nix-build）和新的基于 nix 命令的接口（如 nix build）。这种区别主要是由 Nix 的发展和迭代引起的。
- 传统命令（如 nix-build）：这些是早期 Nix 版本中使用的命令。它们各自独立，如 nix-env、nix-build、nix-shell 等，每个都有其特定的用途。
- 新的命令（如 nix build）：在更近期的 Nix 版本中，开发者引入了一个统一的命令行界面，即 nix 命令，后跟不同的子命令。这种方式更类似于其他现代化的命令行工具，如 git。例如，nix build 是新界面下的构建命令，类似于传统的 nix-build。

[microvm.nix](https://github.com/astro/microvm.nix)

[nixpak](https://github.com/nixpak/nixpak)

[nix-serve](https://github.com/edolstra/nix-serve)

[qubes](https://www.qubes-os.org/intro/)

[NixOS下的Hybrid架构笔记本无诱骗器的显卡直通方案参考](https://lostattractor.net/archives/nixos-gpu-vfio-passthrough)
